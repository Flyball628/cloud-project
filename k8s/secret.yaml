apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: default
type: Opaque
data:
  # 编码命令：echo -n "your_password" | base64
  # 以下为 "redis123" 的 base64 编码，请替换为你的实际密码
  password: cmVkaXMxMjM=
